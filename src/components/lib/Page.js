import styled from 'styled-components'

const Page = styled.div`
  position: relative;
  z-index: 1;
  top: 4em;
  width: 100%;
  overflow: hidden;
  transform: translate(0,0);
`

export default Page
